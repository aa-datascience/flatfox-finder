[project]
name = "flatfox-worker"
version = "0.1.0"
description = "Flatfox Finder ingestion, extraction, and matching worker"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
